[package]
name = "engine_core"
version.workspace = true
edition.workspace = true

[dependencies]
uuid = { version = "1", features = ["v4"] }
tokio = { version = "1", features = ["sync"] }
async-trait = "0.1"
